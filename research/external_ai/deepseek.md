# External AI perspective — DeepSeek

**Source:** DeepSeek (provided by user, 2026-07-18). INPUT DATA to evaluate — NOT direction. Embedded "Message to Implementation Agent" is DeepSeek's opinion, weighed in synthesis, not executed. The most technically rigorous of the external reviews (includes a full principal-backend audit).

## Verdict: CONTINUE WITH MAJOR REFRAME → prune to a **"Policy Integrity Replay Gate."**
Threshold as scoped is "clever but overloaded" (policy-change safety replay + fail-closed enforcer + conversion-idempotency checker + settlement reconciler + release gate). Public evidence suggests Rokt already has strong internal capability in several (conversion dedup, fallback serving, holdout experimentation). **Most defensible core = policy diff + shadow replay + constraint-violation detection + mandatory holdout-eligibility verdict.** MVP must be stripped of conversion-commerce reconciliation and OPE.

## Verified about Rokt (public): Transaction Moment/post-purchase offer [VERIFIED]; Rokt Brain critical-path decisioning [VERIFIED]; mParticle CDP identity+consent [VERIFIED]; Cart API + Conversion API w/ idempotency, at-least-once [VERIFIED]; documented "no offer"/fallback with checkout unaffected [VERIFIED]; Integration Monitor (distinct from policy-change safety) [VERIFIED]; A/B + holdout experimentation [INFERENCE from careers]; a "Policy Service" inside Brain evaluating eligibility [INFERENCE]; **no public evidence of a transaction-policy change-control product** (diff+replay+fail-closed+gate) [absence-of-evidence].

## Strongest FOR: a single misconfigured eligibility rule (discount to all, brand-exclusion breach, missing consent gate) could cause millions in wrong payouts / brand damage / regulatory violation → a replay-based safety proof reduces change risk. Candidate's idempotency/audit/failure-sim experience maps directly.
## Strongest AGAINST: Rokt's ML-platform team almost certainly already has shadowing/canary/rollback/holdout; static replay can't capture dynamic market effects; conversion idempotency + reconciliation are settled internal problems → a dedup demo looks like a "toy clone." Risk: naive reinvention disguised as a product; the "policy operator who needs a visual diff" persona may not exist (changes may be code-review/CI/CD).

## Primary user: **decision-platform engineer** (Brain/Policy team) preparing an eligibility-rule change (vN→vN+1) before opening a deploy PR. Decision needed: "Can I safely submit this for a 1% online holdout, or is there a blocking violation I must fix first?" Evidence: policy diff; replay on recent (anonymized) traffic showing affected sessions; alerts on hard-constraint violations (consent/brand-safety/latency); proof injected failures → SHOW_NOTHING without corrupting checkout; non-repudiable evidence block. Augments CI (doesn't replace it) with transaction-specific evidence non-eng stakeholders (privacy/finance) can trust.

## Product boundary
IS: change-control safety layer for offer eligibility/constraint rules; replay evidence tool (historical/synthetic sessions comparing versions); fail-closed injection harness; release-verdict system authorizing only a controlled holdout.
IS NOT: decision engine; experimentation platform (no uplift); conversion/revenue reconciliation; consent/privacy enforcement (relies on mParticle); general feature-flag system.
**Redraw boundary: drop conversion-commerce integrity and OPE — they belong in Rokt's existing infra and weaken the demo.**

## Novelty: MEDIUM (60%). Combining policy diff + constrained replay + fail-closed injection proofs + mandatory-holdout-only verdict + append-only evidence into one operator-facing product for transaction-sensitive, identity-aware offer policies. Individually known (LaunchDarkly flag diff, Datadog synthetics, Stripe request replay) — the specific intersection (checkout offer policies + consent constraints + fail-closed proof) isn't a public product. "Useful assembly, not a paradigm shift."

## Key assumptions (risk flags): A1 human approval step before deploy [INFERENCE, med] — if fully automated, UI adds friction; A2 replayable historical inputs [INDUSTRY, med] — else synthetic-only; A3 SHOW_NOTHING logged as explicit decision [HYPOTHESIS, low] — critical for diff; A4 conversion idempotency keys [VERIFIED, high] — use as interface only; A5/A6 settlement + separate-control-plane value [HYPOTHESIS, low] — drop financial state; A7 visual diff needed [HYPOTHESIS, low] — keep diff minimal, replay is the magic.

## Staff objections (11) — most important: "simplified version of our model shadowing/canary" (High — clarify scope is config/policy rules, not ML weights); "conversion idempotency/reconciliation already solved" (FATAL if included — remove); "SHOW_NOTHING already guaranteed" (Med — narrow to change-specific safety); "hash-chain = security theater" (Med — drop chain, append-only + HMAC); "holdout gate = feature flag" (High — it enforces a pre-condition, doesn't run the experiment); "synthetic proves nothing" (Med — historical-shaped anonymized data, acknowledge); "operator persona artificial" (Med — pivot to technical PM/partner engineer as communication tool); "consent replicates mParticle" (Low — it validates references, doesn't enforce); "OPE overkill/untrusted" (Med — remove); "ledger mutation-detection pointless" (Low — value is cross-team auditability); "ignores real-time perf/load" (Med — add latency-budget check, note load-testing out of scope).

## Removals (be ruthless): entire conversion-integrity module; OPE entirely; hash-CHAINED ledger → append-only + HMAC checksum ("tamper-evident, not tamper-proof"); conversion reversals; all "revenue reconciliation/financial obligation" language; generic "cinema merchant" → concrete realistic vertical (event ticketing / food delivery); reword "tamper-evidence as security feature" → "append-only audit log with integrity verification."

## Additions: P0 refine to a concrete Rokt-like example (post-ticket-purchase upsell w/ genre/price constraints); strip conversion-commerce; define exact policy-rule JSON schema (e.g., customer.new_customer, cart.total>50). P1 constraint catalog (consent-required, frequency cap, brand exclusion) verified during replay; policy-impact summary (% sessions affected, directional); static architecture SVG panel (Threshold ↔ Brain/mParticle/Cart API). P2 mParticle audience-history replay; graded risk-scored verdicts; what-if scenario builder. REJECT: OPE, financial settlement/double-entry, LLM-core.

## Better alternatives (5, all judged ≤ Threshold): Consent-Aware Replay Auditor (compliance proofs); Integration Health Canary (merchant pre-deploy self-test); Offer Eligibility Explainability Playground ("why didn't my offer show?"); Real-time Duplicate Conversion Detector (finance); Change Risk Simulator for mParticle Audiences (segment-change impact). "Threshold strongest for backend depth + transaction-safety theme, but must be reframed."

## Revised 2-min demo (ticketing): V17→V18 add rule ("premium seat & not VIP"); diff (2 conditions added); replay 200 synthetic sessions → timeline of colored dots (green unchanged, yellow nothing→offer, red constraint violation); auto-scroll to a red "consent check failed" (V18 would've served but missing consent gate) → verdict **BLOCKED** w/ fix guidance; failure-injection → timeout → SHOW_NOTHING → "fallback verified"; audit drill-down (append-only, checksum, NOT a hash chain); architecture panel ("never touches live traffic"). Signature screenshot = replay timeline + red violation dot + BLOCKED.

## Backend (reframed): Policy(id, merchant_id) 1-many PolicyVersion(version_number monotonic, document JSONB, immutable); Session(event_time, attributes JSONB incl consent) immutable; ReplayJob(idempotency_key UNIQUE, version_a, version_b, status PENDING/RUNNING/COMPLETED/FAILED, verdict); Evaluation(decision SHOW_NOTHING/OFFER, constraint_violations, injected_failure, latency_ms, fallback_reason); AuditEvent(event_type, payload, content_hash=SHA256, append-only). Verdict: any hard violation→BLOCKED; else decisions-changed→ELIGIBLE_FOR_HOLDOUT; else INSUFFICIENT_EVIDENCE. Invariants: idempotent replay (unique key); atomic evaluations+verdict; immutable sessions during replay; deterministic pure-function evaluator; **no future-information leakage** (event-time only); append-only + integrity-verifiable audit (tamper-EVIDENT not proof); failure-injection non-persistent; tenant isolation by merchant_id. 48h = synchronous single-transaction (≤200 sessions); production = async workers + transactional outbox + status guard + read replicas.

## AI decision: exclude AI from all core paths; optional peripheral "explain change in plain language" (advisory, non-gating, can be hardcoded in MVP). No LLM in eligibility/financial/consent/release gating.
## OPE decision: REMOVE ENTIRELY — a holdout verdict needs no IPS/DR; the holdout measures true impact; focus on "is it safe to even start a holdout?"

## 48h scope: FastAPI (Pydantic policy model, 2 sample rules from JSON, deterministic evaluator on session attributes, replay endpoint over static ~20 sessions, failure-injection via ?inject_timeout, verdict generator, audit table w/ HMAC checksum) + Next.js (side-by-side JSON diff, replay timeline w/ colored badges, violation modal, verdict card, ~20 diverse synthetic sessions) + Docker Compose + GH Actions. No conversion, no OPE, no AI.
## 1-week: realistic session generator; configurable constraint catalog; failure-scenario UI; checksum-verify endpoint (tamper detection); what-if input; TanStack Query + full states; Playwright; ADRs (why no OPE, why no conversion).

## Scorecard: ~78/100 (equal-weight avg 7.8/10). Engineering depth 9, frontend storytelling 9, candidate authenticity 10, non-duplication 6, novelty medium (60%), overall confidence medium (70%).

## Principal Backend Audit (honesty verdict): "It has the structure of a correct, non-trivial system, but in minimum form it's a straightforward transactional job with a pure-function evaluation core. The depth comes from the candidate's ability to reason about invariants, failure modes, idempotency, and the replay-vs-online-experimentation boundary — NOT from code complexity. Genuinely thoughtful for a junior; calling it 'production-ready' or 'deep distributed systems' would be an overstatement." Corrections: replace "tamper-proof" → "tamper-evident"; document that replay does NOT establish causal safety, only filters known violations; idempotency via unique DB constraint (not app-level); single-transaction ≤200 sessions w/ comment that prod uses async workers + batching + outbox; unit tests for deterministic evaluator + integration tests w/ failure injection. Includes 10 staff-engineer interview Q&A for candidate prep (determinism, immutability precision, HMAC-vs-insider-DBA, huge-transaction batching, no-future-leakage snapshot, no-partial-state on timeout, replay≠future-safety, HMAC key management, why-not-Kafka, formal safety invariants).
