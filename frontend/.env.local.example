# Base URL of the Threshold backend API. Defaults to http://localhost:8000 when unset.
NEXT_PUBLIC_API_BASE=http://localhost:8000
# Optional demo operator identity sent as X-Threshold-User. Defaults to demo-operator.
NEXT_PUBLIC_THRESHOLD_USER=demo-operator
